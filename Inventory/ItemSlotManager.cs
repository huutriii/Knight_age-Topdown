using UnityEngine;
using UnityEngine.UI;

public class ItemSlotManager : MonoBehaviour
{
    [SerializeField] Sprite itemIcon;
    [SerializeField] int slotIndex;
    [SerializeField] GridLayoutGroup group;

    private void Start()
    {

    }

    // GridLayoutGroup-slot(image)-item(image)
}
